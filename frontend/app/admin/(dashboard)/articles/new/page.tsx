"use client";
import { useEffect, useState } from "react"; import { get } from "@/lib/apiClient"; import { ArticleForm } from "@/components/admin/article-form"; import type { CategoryListItem } from "@/lib/admin-types"; export default function NewArticlePage(){const [categories,setCategories]=useState<CategoryListItem[]>([]);useEffect(()=>{get<{data:CategoryListItem[]}>("/api/categories").then(r=>setCategories(r.data));},[]);return <ArticleForm mode="create" categories={categories.map(({id,name,slug,isActive})=>({id,name,slug,isActive}))}/>;}
