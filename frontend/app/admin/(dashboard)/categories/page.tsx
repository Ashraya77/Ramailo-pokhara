"use client";
import { useEffect, useState } from "react"; import { get } from "@/lib/apiClient"; import { CategoryTable } from "@/components/admin/category-table"; import type { CategoryListItem } from "@/lib/admin-types"; export default function CategoriesPage(){const [categories,setCategories]=useState<CategoryListItem[]>([]);useEffect(()=>{get<{data:CategoryListItem[]}>("/api/categories").then(r=>setCategories(r.data));},[]);return <div className="space-y-6"><div><h1 className="text-3xl font-bold tracking-tight">Categories</h1><p className="text-muted-foreground">Manage news portal categories, sorting orders, and statuses.</p></div><CategoryTable initialCategories={categories}/></div>;}
